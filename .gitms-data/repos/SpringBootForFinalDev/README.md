# SpringBootForFinalDev

Java project built with Maven and Spring Boot (based on the `games` module structure and `pom.xml`).

<!-- TODO: describe what this project actually does -->

## What it does

<!-- TODO: entry-point source files could not be read, so the application's behavior is unknown -->

## Tech stack

- Java
- Maven (`pom.xml`, `mvnw` wrapper present)

## Getting started

This repository uses the Maven wrapper included in the `games` directory. From within `games/`:

```
./mvnw spring-boot:run
```

<!-- TODO: confirm this is the correct run command once source files are reviewed -->

## License

No license file is present in this repository.
